package display.components;

/**
 * Interface for components of a screen (display)
 * Each display component must be able to have a
 * draw method.
 */
public interface IDisplayComponent {
    /**
     * Has to draw the component
     */
    void draw();
}
