package System.Aircraft.Airliner;

public interface IPerson {
    public String getName();


    /**
     * method to tell if a person is ginger therefore removing one soul from the airplane
     * Used in later inheritence
     */
    public boolean isGing();
}
